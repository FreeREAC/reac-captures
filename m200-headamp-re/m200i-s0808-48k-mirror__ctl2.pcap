REMOVED BY THE LFS HISTORY REWRITE OF 2026-08-23

This capture was committed RAW and exceeded GitHub's 2 GiB per-object LFS
limit, which made the whole repository unpushable. The pointer was replaced
by this note so the history could be published. The bytes were NOT deleted.

sha256: db531283c15108d1bed5de76cbb26c99bae85c3956252410bdd1ceb738850675
bytes:  4758051208

The raw file is at ~/Devel/audio/reac-captures-raw/, outside git, with a
README and a full SHA256SUMS.txt; the digest above is its entry there. The
DISTILLED capture at the same relative path in the current commit is the one
the two corpus gates read.
